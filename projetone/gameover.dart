void main() {
  {
    print("Game Over");
  }
}

continueGame() {
  int playerHealth = 100; // Starting healthj
  for (int i = 0; i < 10; i++) {
    // Simulating player losing health each iteration
    playerHealth--;

    // Check if player has lost the game
    if (playerHealth <= 0) ;
  }
}
